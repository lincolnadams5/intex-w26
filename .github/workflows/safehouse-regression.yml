name: Weekly Safehouse Regression

on:
  schedule:
    - cron: '0 0 * * 0'   # Every Sunday at midnight UTC
  workflow_dispatch:        # Allow manual runs from the Actions tab

jobs:
  run-regression:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pipeline/safehouse_outcome_drivers

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: pipeline/safehouse_outcome_drivers/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run regression job
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python -m jobs.run_regression
